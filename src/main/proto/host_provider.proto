syntax = "proto3";
package dev.bingo;

service HostService {
  rpc GetHosts (HostRequest) returns (HostResponse);
}

message HostRequest { string original_url = 1; }
message HostResponse { repeated string hosts = 1; }
